{
    "type" : "object",
    "properties": {
        "op" : {
            "type" : "string",
            "enum" : ["add", "replace", "remove"]
        },
        "path" : {
            "type": "string"
        },
        "value" : {
            "type" : ["number", "string", "boolean"]
        }
    },
    "required": ["op", "path"],
    "additionalProperties": false
}